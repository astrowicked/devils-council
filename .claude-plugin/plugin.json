{
  "name": "devils-council",
  "version": "1.7.0",
  "description": "Get a staff-level LGTM — 4 expert personas (Staff Engineer, SRE, PM, Devil's Advocate) plus auto-triggered bench personas review plans, code, and RFCs in parallel. Council Chair synthesizes contradictions without collapsing dissent.",
  "author": {
    "name": "Andy Woodard",
    "url": "https://github.com/astrowicked"
  },
  "homepage": "https://github.com/astrowicked/devils-council",
  "repository": "https://github.com/astrowicked/devils-council",
  "license": "MIT",
  "keywords": [
    "review",
    "personas",
    "adversarial-review",
    "plan-review",
    "code-review",
    "gsd",
    "claude-code-plugin"
  ],
  "userConfig": {
    "gsd_integration": {
      "type": "boolean",
      "title": "GSD integration",
      "description": "After GSD plan-checker or code-reviewer completes, suggest a devils-council review command. Requires GSD installed.",
      "default": true
    },
    "shell_inject_guard": {
      "type": "boolean",
      "title": "Shell-inject guard",
      "description": "Enable PreToolUse dry-run pre-parser on commands/*.md edits. Blocks Write/Edit operations that introduce unauthorized inline !`<cmd>` or fenced ``` ! shell-inject patterns (class of bug that shipped as v1.0.0 P0). Allowlist: scripts/shell-inject-allowlist.txt + inline <!-- dc-shell-inject-ok: reason --> markers. Set to false to disable the hook (not recommended; CI step still enforces on push).",
      "default": true
    }
  }
}
