---
phase: 03-signal-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .opencode/agents/security-reviewer.md
  - .opencode/agents/finops-auditor.md
  - .opencode/agents/air-gap-reviewer.md
  - .opencode/agents/performance-reviewer.md
  - .opencode/agents/council-review.md
  - .opencode/build.sh
  - .opencode/test-fixtures/aws-plan.md
  - .opencode/test-fixtures/simple-refactor.md
autonomous: true
requirements: [OC-SIGNAL-01, OC-BENCH-01]

must_haves:
  truths:
    - "4 bench persona agents (security-reviewer, finops-auditor, air-gap-reviewer, performance-reviewer) exist in .opencode/agents/ with adapted OpenCode bodies"
    - "council-review.md includes signal detection rules that instruct the LLM to activate bench persona lenses based on artifact patterns"
    - "build.sh transforms the 4 bench personas from agents/ source through the same pipeline as core personas"
    - "Signal detection rules are specific enough to match real artifact patterns (proven via test fixtures)"
    - "No Codex delegation or Claude Code-specific references remain in bench persona agents"
  artifacts:
    - path: ".opencode/agents/security-reviewer.md"
      provides: "Security bench persona for OpenCode"
      contains: "persona: security-reviewer"
    - path: ".opencode/agents/finops-auditor.md"
      provides: "FinOps bench persona for OpenCode"
      contains: "persona: finops-auditor"
    - path: ".opencode/agents/air-gap-reviewer.md"
      provides: "Air-gap bench persona for OpenCode"
      contains: "persona: air-gap-reviewer"
    - path: ".opencode/agents/performance-reviewer.md"
      provides: "Performance bench persona for OpenCode"
      contains: "persona: performance-reviewer"
    - path: ".opencode/test-fixtures/aws-plan.md"
      provides: "Positive test fixture for signal detection rules"
      contains: "boto3"
    - path: ".opencode/test-fixtures/simple-refactor.md"
      provides: "Negative test fixture proving no false-positive signal activation"
  key_links:
    - from: ".opencode/agents/council-review.md"
      to: "bench persona signal rules"
      via: "inline signal→persona mapping instructions in agent body"
      pattern: "security-reviewer.*auth.*crypto"
    - from: ".opencode/test-fixtures/aws-plan.md"
      to: ".opencode/agents/council-review.md"
      via: "fixture patterns match documented FinOps Auditor activation criteria"
      pattern: "boto3|@aws-sdk"
---

<objective>
Port 4 bench personas to OpenCode and implement soft signal detection — LLM-driven rules in council-review.md that activate bench persona lenses based on artifact content patterns.

Purpose: Enable bench persona activation based on artifact content patterns. The council-review orchestrator needs to know WHICH additional lenses to apply. Phase 3 delivers soft detection (LLM-driven rules). Phase 4 will add deterministic TypeScript detection when the /review command is built — building the classifier here without a consumer would produce dead code.

Dual-Deploy reviewer deferred to v1.3 — highly specific to KOTS/SaaS hybrid; the 4 selected personas cover universally applicable concerns.

Output: 4 new bench persona agents + updated council-review.md with signal rules + behavioral test fixtures proving rule specificity
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-persona-adaptation/02-01-SUMMARY.md

@.opencode/plugins/devils-council.ts
@.opencode/agents/council-review.md
@.opencode/build.sh
@lib/signals.json

<interfaces>
<!-- Key contracts from Phase 2 that this plan builds upon -->

From .opencode/build.sh:
```bash
# PERSONAS array controls which agents/ sources get transformed
PERSONAS=(staff-engineer sre product-manager devils-advocate council-chair)
# The script: reads agents/<name>.md → transforms frontmatter + body → writes .opencode/agents/<name>.md
```

From .opencode/agents/council-review.md (Phase 2 output):
```markdown
---
description: "Experimental — single-context sequential review..."
mode: subagent
permission:
  edit: deny
  bash: deny
---
# 4 phases (staff-engineer, sre, product-manager, devils-advocate) + Chair synthesis
```

From lib/signals.json (signal→persona mapping — used as reference for LLM rules, not imported at runtime):
```json
{
  "auth_code_change": { "target_personas": ["security-reviewer"] },
  "crypto_import": { "target_personas": ["security-reviewer"] },
  "secret_handling": { "target_personas": ["security-reviewer"] },
  "dependency_update": { "target_personas": ["security-reviewer", "air-gap-reviewer"] },
  "aws_sdk_import": { "target_personas": ["finops-auditor"] },
  "new_cloud_resource": { "target_personas": ["finops-auditor"] },
  "network_egress": { "target_personas": ["air-gap-reviewer"] },
  "external_image_pull": { "target_personas": ["air-gap-reviewer"] },
  "unpinned_dependency": { "target_personas": ["air-gap-reviewer"] },
  "performance_hotpath": { "target_personas": ["performance-reviewer"] }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port 4 bench persona agents via build.sh pipeline</name>
  <files>.opencode/build.sh, .opencode/agents/security-reviewer.md, .opencode/agents/finops-auditor.md, .opencode/agents/air-gap-reviewer.md, .opencode/agents/performance-reviewer.md</files>
  <action>
Add the 4 bench persona names to the PERSONAS array in build.sh:
```bash
PERSONAS=(staff-engineer sre product-manager devils-advocate council-chair security-reviewer finops-auditor air-gap-reviewer performance-reviewer)
```

Run `./build.sh` to transform these 4 bench personas from `agents/` source into `.opencode/agents/`. The existing Python transform handles:
- Frontmatter → OpenCode format (description, mode: subagent, permission: edit/bash deny)
- Body: removes $RUN_DIR references, rewrites output contract to direct-response, removes artifact_sha256, adds OpenCode input instruction

After build, verify each generated agent file has:
- Valid YAML frontmatter with `mode: subagent`
- No remaining `$RUN_DIR` references
- Scorecard output instructions say "Output your scorecard directly in your response"
- Banned phrases inlined (self-contained, no sidecar reference)

Post-build cleanup — manually remove/adapt Claude Code-specific content:
- security-reviewer: Remove the "Delegating a deep scan to Codex" section entirely (Codex deferred to v1.3 per project constraint). Also remove any `delegation_request` blocks.
- All 4: Verify `persona-metadata/*.yml` sidecar references are removed (inlined by build or manually strip)
- All 4: Strip any remaining `codex` references or delegation patterns
  </action>
  <verify>
    <automated>cd /Users/andywoodard/dev/devils-council/.opencode && ./build.sh && for f in agents/security-reviewer.md agents/finops-auditor.md agents/air-gap-reviewer.md agents/performance-reviewer.md; do echo "--- $f ---"; grep -c 'mode: subagent' "$f" | grep -q '^1$' && echo "PASS: mode subagent" || echo "FAIL: missing mode subagent"; count=$(grep -c '\$RUN_DIR' "$f"); [ "$count" -eq 0 ] && echo "PASS: no RUN_DIR" || echo "FAIL: RUN_DIR present ($count)"; done && echo "--- Codex stripping ---" && grep -c 'Codex' agents/security-reviewer.md | grep -q '^0$' && echo "PASS: no Codex in security-reviewer" || echo "FAIL: Codex still in security-reviewer" && total_delegation=$(grep -cl 'delegation_request' agents/security-reviewer.md agents/finops-auditor.md agents/air-gap-reviewer.md agents/performance-reviewer.md 2>/dev/null | wc -l); [ "$total_delegation" -eq 0 ] && echo "PASS: no delegation_request in bench agents" || echo "FAIL: delegation_request found in $total_delegation files"</automated>
  </verify>
  <done>4 bench persona agent files exist in .opencode/agents/ with valid OpenCode frontmatter, no $RUN_DIR references, direct-response output contract, no Codex delegation sections, and no delegation_request blocks</done>
</task>

<task type="auto">
  <name>Task 2: Add signal detection rules to council-review.md with behavioral test fixtures</name>
  <files>.opencode/agents/council-review.md, .opencode/test-fixtures/aws-plan.md, .opencode/test-fixtures/simple-refactor.md</files>
  <action>
Update `.opencode/agents/council-review.md` to include a new section BEFORE the existing "## Process" section. This section tells the LLM which additional bench persona lenses to activate based on observed patterns in the artifact.

**Signal detection rules use simple keyword/pattern matching described in natural language — not regex.** The LLM reads the artifact and checks for the described patterns using its own comprehension. There is no regex engine involved in the soft detection path; these are human-readable activation criteria that the LLM pattern-matches against conversationally. (Complex regex concerns are moot here — this is LLM-driven text understanding, not programmatic pattern matching.)

Insert after the "## Input" section and before "## Process":

```markdown
## Bench Persona Activation (Signal Detection)

After reading the artifact, check for these structural signals. If ANY signals match, add the corresponding bench persona lens as an ADDITIONAL phase after Phase 4 (Devil's Advocate) and before the Chair Synthesis.

### Security Reviewer — activate if you observe:
- Authentication/session/login/JWT/OAuth code or endpoint paths (`/login`, `/auth`, `/oauth`)
- Cryptographic imports (crypto, bcrypt, argon2, jose, libsodium, nacl)
- Secret handling (process.env.*_SECRET, *_KEY, *_TOKEN; secret manager API calls)
- Dependency updates in lockfiles (package-lock.json, yarn.lock, go.sum, etc.)

### FinOps Auditor — activate if you observe:
- AWS SDK imports (boto3, @aws-sdk/client-*, aws-sdk)
- New cloud resource declarations (Terraform resource blocks, CDK constructs, CloudFormation)
- Autoscaling/HPA/replica changes
- Storage class changes

### Air-Gap Reviewer — activate if you observe:
- Dependency updates (same trigger as Security above)
- Network egress to external hosts (fetch/axios/requests to non-localhost URLs)
- External container image pulls (FROM <registry>/..., image: <external>/...)
- Unpinned dependencies (^, ~, >=, latest)
- License/telemetry phone-home (Sentry, Datadog, Mixpanel SDK inits)

### Performance Reviewer — activate if you observe (need 2+ patterns):
- Database/fetch calls inside loops (N+1 query patterns)
- Nested for-loops over collections
- Per-iteration allocations (new Array/Object/Map inside loops)

---

**If no signals match:** Run only the 4 core personas (Staff Engineer, SRE, PM, Devil's Advocate) + Chair.

**If signals match:** Add each triggered bench persona as a new Phase between Phase 4 and Chair Synthesis. Use the SAME scorecard format. Adopt that persona's voice fully (see the standalone agent files for voice reference: @security-reviewer, @finops-auditor, @air-gap-reviewer, @performance-reviewer).
```

Also update the "## Process" section intro to note: "You will execute 4+ sequential phases (4 core + any triggered bench personas), then a synthesis phase."

---

**Create behavioral test fixtures** to prove the signal detection rules are specific enough:

**Positive fixture** — `.opencode/test-fixtures/aws-plan.md`:
A fake plan artifact that SHOULD trigger FinOps Auditor and Air-Gap Reviewer. Content includes:
- An import of `boto3` and `@aws-sdk/client-s3`
- A Terraform `resource "aws_s3_bucket"` block
- An `image: docker.io/nginx:latest` container reference (unpinned, external)
- A `fetch("https://api.datadog.com/v1/series")` telemetry call

This fixture should clearly match FinOps Auditor criteria (boto3, aws-sdk, Terraform resource) AND Air-Gap Reviewer criteria (external image, unpinned, telemetry phone-home).

**Negative fixture** — `.opencode/test-fixtures/simple-refactor.md`:
A fake plan artifact that should NOT trigger ANY bench persona. Content includes:
- Renaming a local variable from `oldName` to `newName`
- Moving a function from `utils.ts` to `helpers.ts`
- Adding a JSDoc comment
- No imports, no cloud resources, no auth, no external URLs, no loops

This fixture should activate ZERO bench personas — only the 4 core run.
  </action>
  <verify>
    <automated>cd /Users/andywoodard/dev/devils-council/.opencode && echo "--- council-review.md signal rules ---" && grep -c "Bench Persona Activation" agents/council-review.md && grep -c "Security Reviewer" agents/council-review.md && grep -c "FinOps Auditor" agents/council-review.md && grep -c "Air-Gap Reviewer" agents/council-review.md && grep -c "Performance Reviewer" agents/council-review.md && echo "--- Positive fixture validation ---" && grep -q "boto3" test-fixtures/aws-plan.md && echo "PASS: boto3 in positive fixture" || echo "FAIL: missing boto3" && grep -q "@aws-sdk" test-fixtures/aws-plan.md && echo "PASS: aws-sdk in positive fixture" || echo "FAIL: missing aws-sdk" && grep -q "docker.io" test-fixtures/aws-plan.md && echo "PASS: external image in positive fixture" || echo "FAIL: missing external image" && grep -q "datadog" test-fixtures/aws-plan.md && echo "PASS: telemetry in positive fixture" || echo "FAIL: missing telemetry" && echo "--- Negative fixture validation (should NOT match signal keywords) ---" && ! grep -qi "boto3\|aws-sdk\|crypto\|bcrypt\|oauth\|docker.io\|fetch(\"https" test-fixtures/simple-refactor.md && echo "PASS: negative fixture has no signal keywords" || echo "FAIL: negative fixture contains signal keywords"</automated>
  </verify>
  <done>council-review.md contains signal detection rules section with all 4 bench persona activation criteria (human-readable, not regex), updated process description acknowledging variable phase count, positive test fixture proving FinOps+AirGap activation patterns match, and negative test fixture proving no false-positive on benign refactor artifacts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User artifact → council-review LLM rules | Untrusted text read by LLM for pattern matching against signal rules |
| Signal activation → bench persona invocation | Triggered persona names add review phases (non-destructive expansion) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | council-review signal rules | accept | LLM-driven detection is advisory; false activation produces extra review (non-destructive safe failure mode), false negatives miss a bench lens (acceptable for soft detection — deterministic TypeScript path will be authoritative in Phase 4) |
| T-03-02 | Information Disclosure | bench persona agents | accept | Agents have permission edit:deny bash:deny — cannot read or modify user files beyond the artifact provided in conversation context |
| T-03-03 | Spoofing | council-review.md signal rules | accept | An attacker could craft artifacts to avoid signal triggering, but the cost is extra review, not bypassing security — no privilege escalation possible through false negatives in an advisory review tool |
</threat_model>

<verification>
1. `./build.sh` completes without error and produces all 9 persona agents in `.opencode/agents/`
2. Each bench persona agent has valid OpenCode frontmatter, no Claude Code-specific references
3. `grep -c 'Codex' .opencode/agents/security-reviewer.md` returns 0
4. `grep -cl 'delegation_request' .opencode/agents/*.md` returns no bench agent matches
5. `council-review.md` includes the signal detection rules section with all 4 bench persona criteria
6. No `$RUN_DIR`, no `persona-metadata/*.yml` sidecar references in any `.opencode/agents/*.md` file
7. Positive test fixture (`aws-plan.md`) contains patterns matching FinOps Auditor AND Air-Gap Reviewer activation criteria
8. Negative test fixture (`simple-refactor.md`) contains NO patterns matching any bench persona activation criteria
</verification>

<success_criteria>
- 4 bench persona agents usable via `@security-reviewer`, `@finops-auditor`, `@air-gap-reviewer`, `@performance-reviewer` in OpenCode
- council-review orchestrator dynamically activates bench lenses based on artifact content patterns via LLM-driven soft detection
- Test fixtures prove signal rules are specific (match real patterns) and not overly broad (don't false-positive on everything)
- All 10 agent files (6 existing + 4 new) pass basic validation: valid frontmatter, no filesystem references, no Codex/delegation artifacts
- TypeScript signal classifier deferred to Phase 4 where it has an actual consumer (the /review command)
</success_criteria>

<output>
After completion, create `.planning/phases/03-signal-detection/03-01-SUMMARY.md`
</output>
