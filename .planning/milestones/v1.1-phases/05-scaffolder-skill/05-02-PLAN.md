---
phase: 05-scaffolder-skill
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - README.md
  - CHANGELOG.md
autonomous: true
requirements: [SCAF-05]

must_haves:
  truths:
    - "README documents the scaffolder workflow including invoke command, workspace path, and move-to-project instructions"
    - "CHANGELOG v1.1 lists scaffolder as a new capability"
  artifacts:
    - path: "README.md"
      provides: "Scaffolder documentation section"
      contains: "create-persona"
    - path: "CHANGELOG.md"
      provides: "v1.1 scaffolder changelog entry"
      contains: "create-persona"
  key_links:
    - from: "README.md"
      to: "skills/create-persona/SKILL.md"
      via: "Documentation references the skill command"
      pattern: "create-persona"
---

<objective>
Document the scaffolder in README and CHANGELOG.

Purpose: Users need to know the scaffolder exists, how to invoke it, what it produces, and where to find the output. CHANGELOG provides release-note visibility for v1.1.

Output: Updated README.md with scaffolder workflow section, updated CHANGELOG.md with v1.1 scaffolder entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-scaffolder-skill/05-CONTEXT.md
@.planning/phases/05-scaffolder-skill/05-01-SUMMARY.md

@README.md
@CHANGELOG.md
@skills/create-persona/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scaffolder workflow section to README</name>
  <files>README.md</files>
  <read_first>
    README.md
    skills/create-persona/SKILL.md
    .planning/phases/05-scaffolder-skill/05-CONTEXT.md
  </read_first>
  <action>
Add a new section `## Custom Personas` to README.md, placed AFTER the existing `## Configuration` section (line ~149) and BEFORE `## Codex Setup` (line ~176). This placement puts it logically between "how to configure the plugin" and "how to set up Codex" — custom persona creation is a configuration-adjacent activity.

The section content (exact text, adapt only line breaks for readability):

```markdown
## Custom Personas

Create a custom persona interactively:

```
/devils-council:create-persona
```

Or provide a slug directly:

```
/devils-council:create-persona cost-hawk
```

The scaffolder walks you through every voice-kit field:

1. **Name** (kebab-case slug)
2. **Tier** (core = always-on, bench = signal-triggered)
3. **Primary concern** (one-sentence value-system anchor ending with `?`)
4. **Blind spots** (what this persona does NOT care about)
5. **Characteristic objections** (3+ verbatim phrases)
6. **Banned phrases** (5+ phrases the persona must never use)
7. **Worked examples** (2 good findings + 1 bad finding)

The scaffolder:
- Suggests available signal IDs for bench triggers
- Warns if your banned phrases overlap >30% with a shipped persona
- Flags objections that contain your own banned phrases
- Previews the full file before writing
- Validates against `scripts/validate-personas.sh` before declaring success

Output lands in:

```
${CLAUDE_PLUGIN_DATA}/create-persona-workspace/<slug>/
  agents/<slug>.md
  persona-metadata/<slug>.yml
```

To install into your plugin:

```bash
cp "${CLAUDE_PLUGIN_DATA}/create-persona-workspace/<slug>/agents/<slug>.md" agents/
cp "${CLAUDE_PLUGIN_DATA}/create-persona-workspace/<slug>/persona-metadata/<slug>.yml" persona-metadata/
./scripts/validate-personas.sh
```

Then run `/reload-plugins` to pick up the new persona.

> **Note:** v1.2 will add `userConfig.custom_personas_dir` so custom personas survive plugin updates without manual copying.
```

Also update the existing `## Commands` section to add a row to the commands table (the table that lists `/devils-council:review`, `/devils-council:on-plan`, etc.):

```markdown
| `/devils-council:create-persona [slug]` | Interactive wizard to scaffold a custom persona with voice-kit coaching |
```

Insert this row at the END of the existing commands table, after the last current entry.
  </action>
  <verify>
    <automated>
grep -q "## Custom Personas" README.md && \
grep -q "create-persona-workspace" README.md && \
grep -q "create-persona" README.md && \
grep -q "CLAUDE_PLUGIN_DATA" README.md && \
grep -q "validate-personas.sh" README.md && \
echo "PASS: README scaffolder section present" || echo "FAIL: missing scaffolder docs"
    </automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `## Custom Personas` section header
    - Section appears after `## Configuration` and before `## Codex Setup`
    - Section contains `/devils-council:create-persona` invocation example
    - Section documents workspace path `${CLAUDE_PLUGIN_DATA}/create-persona-workspace/<slug>/`
    - Section contains install commands (`cp` commands for agents/ and persona-metadata/)
    - Section mentions `validate-personas.sh` validation
    - Section mentions `/reload-plugins`
    - Section contains v1.2 note about `userConfig.custom_personas_dir`
    - Commands table contains `create-persona` row
    - No existing README content is deleted or reordered (additive only)
  </acceptance_criteria>
  <done>
    README documents the full scaffolder workflow: invocation, field list, coaching features, workspace output path, install commands, and v1.2 teaser.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add scaffolder entry to CHANGELOG</name>
  <files>CHANGELOG.md</files>
  <read_first>
    CHANGELOG.md
    skills/create-persona/SKILL.md
  </read_first>
  <action>
Add a new entry under the existing `## [Unreleased]` section's `### Added` subsection in CHANGELOG.md.

Insert AFTER the existing TD-07 entry (the last item currently under `### Added` in `[Unreleased]`), a new bullet:

```markdown
- **Custom persona scaffolder** (`/devils-council:create-persona`) — interactive `AskUserQuestion`-driven wizard that walks users through all voice-kit fields (name, tier, primary concern, blind spots, objections, banned phrases, worked examples), coaches voice distinctness by detecting >30% banned-phrase overlap with shipped personas, validates output against `scripts/validate-personas.sh` before writing, and outputs to `${CLAUDE_PLUGIN_DATA}/create-persona-workspace/<slug>/` with ready-to-run install commands. (SCAF-01 through SCAF-05)
```

Do NOT modify any other CHANGELOG entries. This is additive only.
  </action>
  <verify>
    <automated>
grep -q "Custom persona scaffolder" CHANGELOG.md && \
grep -q "create-persona" CHANGELOG.md && \
grep -q "SCAF-01" CHANGELOG.md && \
echo "PASS: CHANGELOG scaffolder entry present" || echo "FAIL: missing CHANGELOG entry"
    </automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md `[Unreleased]` > `### Added` contains "Custom persona scaffolder" entry
    - Entry references `/devils-council:create-persona` command name
    - Entry references SCAF-01 through SCAF-05 requirement IDs
    - Entry mentions AskUserQuestion, voice distinctness coaching, validate-personas.sh, workspace path
    - No existing CHANGELOG entries are modified or deleted
    - The entry appears under `[Unreleased]` (not under any versioned section)
  </acceptance_criteria>
  <done>
    CHANGELOG documents the scaffolder as a new v1.1 capability with full feature description and requirement traceability.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only plan; no trust boundaries crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information Disclosure | README.md | accept | README is a public repo file; no secrets. Workspace path uses `${CLAUDE_PLUGIN_DATA}` variable, not a concrete user path. |
</threat_model>

<verification>
1. README contains `## Custom Personas` section with scaffolder workflow
2. CHANGELOG `[Unreleased]` contains scaffolder entry
3. No existing content modified or deleted in either file
</verification>

<success_criteria>
- README documents scaffolder invocation, field list, coaching, workspace, and install commands
- CHANGELOG lists scaffolder as a new v1.1 Added capability
- Both files reference `/devils-council:create-persona`
</success_criteria>

<output>
After completion, create `.planning/phases/05-scaffolder-skill/05-02-SUMMARY.md`
</output>
