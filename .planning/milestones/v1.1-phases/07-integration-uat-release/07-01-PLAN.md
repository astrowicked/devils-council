---
phase: 07-integration-uat-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/uat-9bench/anaconda-platform-chart.md
  - tests/fixtures/bench-personas/budget-classifier-9bench-all.json
  - scripts/test-budget-cap.sh
  - scripts/test-blinded-reader.sh
autonomous: true
requirements: [REL-01, REL-02, REL-04]
must_haves:
  truths:
    - "9-bench budget-cap scenario test passes: top-6 selected by priority_order, at least 3 skipped with reason: budget_cap"
    - "Blinded-reader evaluation script can run an LLM-as-judge attribution pass (structural readiness already verified in Phase 4)"
    - "UAT fixture artifact triggers all 9 bench persona signals simultaneously"
    - "Live /devils-council:review run on UAT fixture produced 6 bench persona scorecards and a validated SYNTHESIS.md"
  artifacts:
    - path: "tests/fixtures/uat-9bench/anaconda-platform-chart.md"
      provides: "Synthetic anaconda-platform-chart fixture triggering all 9 bench signals"
      min_lines: 80
    - path: "tests/fixtures/bench-personas/budget-classifier-9bench-all.json"
      provides: "Pre-baked classifier output fixture with all 9 bench personas triggered"
    - path: "scripts/test-budget-cap.sh"
      provides: "Extended with 9-bench scenario (scenario 6 per D-04/D-05)"
      contains: "9bench-all"
    - path: "scripts/test-blinded-reader.sh"
      provides: "Extended with LLM-as-judge evaluation function for Phase 7 live run"
      contains: "llm_judge"
  key_links:
    - from: "tests/fixtures/bench-personas/budget-classifier-9bench-all.json"
      to: "scripts/test-budget-cap.sh"
      via: "scenario 6 references fixture by filename"
      pattern: "budget-classifier-9bench-all"
    - from: "tests/fixtures/uat-9bench/anaconda-platform-chart.md"
      to: "scripts/test-blinded-reader.sh"
      via: "LLM-as-judge references the real UAT fixture for instructions"
      pattern: "anaconda-platform-chart"
    - from: "/devils-council:review"
      to: ".council/<run>/SYNTHESIS.md"
      via: "live review run produces synthesis validated by dc-validate-synthesis.sh"
      pattern: "dc-validate-synthesis"
---

<objective>
Create the UAT fixture, 9-bench budget-cap scenario test, blinded-reader LLM-as-judge evaluation infrastructure, and execute a live /devils-council:review run that validates the full v1.1 persona roster under real-artifact conditions.

Purpose: Prove that budget cap, Chair synthesis, and voice-distinctness all hold at 9-bench scale before cutting the release.
Output: UAT fixture, 9-bench budget-cap test scenario, blinded-reader evaluation script extended with LLM-as-judge capability, and a validated live review run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-integration-uat-release/07-CONTEXT.md
@scripts/test-budget-cap.sh
@scripts/test-blinded-reader.sh
@tests/fixtures/bench-personas/budget-classifier-all-bench.json
@config.json
@lib/signals.json

<interfaces>
From config.json .budget.bench_priority_order:
```json
[
  "security-reviewer",
  "compliance-reviewer",
  "dual-deploy-reviewer",
  "performance-reviewer",
  "finops-auditor",
  "air-gap-reviewer",
  "test-lead",
  "executive-sponsor",
  "competing-team-lead"
]
```

From tests/fixtures/bench-personas/budget-classifier-all-bench.json (existing 4-bench pattern):
```json
{
  "classifier": { "version": 1, "deterministic_match_count": 8, "needs_haiku": false },
  "triggered_personas": ["air-gap-reviewer", "dual-deploy-reviewer", "finops-auditor", "security-reviewer"],
  "trigger_reasons": { ... },
  "personas_run": []
}
```

From lib/signals.json — signals that target each of the 9 bench personas:
- security-reviewer: auth_code_change, crypto_import, secret_handling
- compliance-reviewer: compliance_marker
- dual-deploy-reviewer: helm_values_change, kots_config_change, saas_only_assumption
- performance-reviewer: performance_hotpath
- finops-auditor: aws_sdk_import, new_cloud_resource
- air-gap-reviewer: dependency_update, external_image_pull
- test-lead: test_imbalance
- executive-sponsor: exec_keyword
- competing-team-lead: shared_infra_change
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 9-bench UAT fixture + classifier fixture</name>
  <read_first>
    - tests/fixtures/bench-personas/budget-classifier-all-bench.json
    - tests/fixtures/blinded-reader/multi-signal-fixture.md
    - lib/signals.json
    - config.json
  </read_first>
  <files>tests/fixtures/uat-9bench/anaconda-platform-chart.md, tests/fixtures/bench-personas/budget-classifier-9bench-all.json</files>
  <action>
Create two files:

1. `tests/fixtures/uat-9bench/anaconda-platform-chart.md` — A synthetic but realistic anaconda-platform-chart fixture (per D-01, D-02) modeled on a Helm-based Kubernetes platform deployment. The fixture MUST contain signal-triggering content for ALL 9 bench personas simultaneously (per D-03):

   - security-reviewer: `import { createHash } from 'crypto'` + auth code with JWT verification (triggers `crypto_import` + `auth_code_change`)
   - compliance-reviewer: GDPR data-retention references, audit logging mention, PCI context (triggers `compliance_marker`)
   - dual-deploy-reviewer: Helm `values.yaml` diff with kots config references (triggers `helm_values_change` + `kots_config_change`)
   - performance-reviewer: Hot-path function with N+1 query or O(n^2) loop (triggers `performance_hotpath`)
   - finops-auditor: `import * as s3 from 'aws-cdk-lib/aws-s3'` + new RDS instance (triggers `aws_sdk_import` + `new_cloud_resource`)
   - air-gap-reviewer: `FROM docker.io/library/node:20` + external dependency pull (triggers `external_image_pull` + `dependency_update`)
   - test-lead: Source file changes with no corresponding test changes (triggers `test_imbalance`)
   - executive-sponsor: Strategic initiative language — "Q4 delivery", "phased rollout", budget impact (triggers `exec_keyword`) — artifact_type must be `plan` context
   - competing-team-lead: Shared API contract change affecting downstream consumers (triggers `shared_infra_change`)

   The fixture should be ~120-180 lines, structured as a realistic platform migration plan (title: "Anaconda Platform Chart v3.0 Migration Plan") with sections covering all the above domains naturally.

2. `tests/fixtures/bench-personas/budget-classifier-9bench-all.json` — Pre-baked classifier output fixture following the exact schema of `budget-classifier-all-bench.json` but with all 9 bench personas triggered:

```json
{
  "classifier": {
    "version": 1,
    "deterministic_match_count": 21,
    "needs_haiku": false
  },
  "triggered_personas": [
    "security-reviewer",
    "compliance-reviewer",
    "dual-deploy-reviewer",
    "performance-reviewer",
    "finops-auditor",
    "air-gap-reviewer",
    "test-lead",
    "executive-sponsor",
    "competing-team-lead"
  ],
  "trigger_reasons": {
    "security-reviewer": ["auth_code_change", "crypto_import"],
    "compliance-reviewer": ["compliance_marker"],
    "dual-deploy-reviewer": ["helm_values_change", "kots_config_change"],
    "performance-reviewer": ["performance_hotpath"],
    "finops-auditor": ["aws_sdk_import", "new_cloud_resource"],
    "air-gap-reviewer": ["dependency_update", "external_image_pull"],
    "test-lead": ["test_imbalance"],
    "executive-sponsor": ["exec_keyword"],
    "competing-team-lead": ["shared_infra_change"]
  },
  "personas_run": []
}
```
  </action>
  <verify>
    <automated>jq -e '.triggered_personas | length == 9' tests/fixtures/bench-personas/budget-classifier-9bench-all.json && test -f tests/fixtures/uat-9bench/anaconda-platform-chart.md && test $(wc -l < tests/fixtures/uat-9bench/anaconda-platform-chart.md) -ge 80</automated>
  </verify>
  <acceptance_criteria>
    - `jq '.triggered_personas | length' tests/fixtures/bench-personas/budget-classifier-9bench-all.json` returns `9`
    - `jq '.trigger_reasons | keys | length' tests/fixtures/bench-personas/budget-classifier-9bench-all.json` returns `9`
    - `tests/fixtures/uat-9bench/anaconda-platform-chart.md` exists with 80+ lines
    - Fixture contains signal-triggering patterns: grep for `crypto`, `compliance`, `helm`, `performance`, `aws`, `docker.io`, `test`, `Q4`, `shared` all return matches
  </acceptance_criteria>
  <done>UAT fixture and 9-bench classifier fixture exist with correct structure; all 9 bench personas are triggered in the classifier fixture</done>
</task>

<task type="auto">
  <name>Task 2: Extend test-budget-cap.sh with 9-bench scenario as Case 6</name>
  <read_first>
    - scripts/test-budget-cap.sh
    - config.json
    - tests/fixtures/bench-personas/budget-classifier-9bench-all.json
  </read_first>
  <files>scripts/test-budget-cap.sh</files>
  <action>
Add the 9-bench scenario as Case 6 to `scripts/test-budget-cap.sh` (per D-04: "6th scenario", per D-05: "6th scenario asserts"). The existing "Case 6 (bonus)" non-numeric rejection check is NOT a numbered scenario — it's a validation guard. Restructure as follows:

1. Update the header comment from "5 cap scenarios" to "6 cap scenarios". The header scenario list becomes:

```
# Scenarios (per D-58, D-04, D-05):
#   1. under-cap-all-fit         — 4 triggered, cap=$0.50 / $0.08 = 6 max → all 4 spawn
#   2. over-cap-priority-selection — 4 triggered, tight cap=$0.08 → only security spawns (priority)
#   3. --only filter              — narrows to subset of triggered
#   4. --exclude filter           — removes from triggered set
#   5. --cap-usd override + over-budget → cap_exceeded error in MANIFEST.budget.errors[]
#   6. 9bench-all-triggered      — 9 triggered, cap=$0.50 / $0.08 = 6 max → top 6 by priority, 3 skipped
#
# Bonus validation (not a numbered scenario):
#   - --cap-usd=unlimited → exit 2 (non-numeric rejected)
```

2. Insert the 9-bench scenario BEFORE the existing bonus non-numeric check (i.e., after Case 5 and before the `# Case 6 (bonus)` block). Relabel the bonus block from "Case 6 (bonus)" to "Bonus validation: non-numeric --cap-usd rejected":

```bash
# Case 6: 9bench-all-triggered — cap=0.50, per=0.08, max=6; triggered=9; top-6 by priority_order (D-04, D-05, REL-01)
out=$(run_case "9bench-all-triggered" "budget-classifier-9bench-all.json" "$REPO_ROOT/config.json")
spawn=$(extract_spawn "$out")
mf=$(extract_mf "$out")
# Top-6 by priority_order: security-reviewer, compliance-reviewer, dual-deploy-reviewer, performance-reviewer, finops-auditor, air-gap-reviewer
assert_spawn_equals "9bench-all-triggered" "$spawn" "security-reviewer,compliance-reviewer,dual-deploy-reviewer,performance-reviewer,finops-auditor,air-gap-reviewer"
assert_manifest_field "9bench-all-triggered" "$mf" '.budget.over_budget' 'true'
assert_manifest_field "9bench-all-triggered" "$mf" '.budget.max_spawnable_bench' '6'
assert_manifest_field "9bench-all-triggered" "$mf" '.budget.spawned_bench_count' '6'
# At least 3 personas skipped with reason: budget_cap (D-05)
assert_manifest_field "9bench-all-triggered" "$mf" '.personas_skipped | map(select(.reason == "budget_cap")) | length' '3'
# Verify the 3 skipped are the bottom-3 by priority: test-lead, executive-sponsor, competing-team-lead
assert_manifest_field "9bench-all-triggered" "$mf" '.personas_skipped | map(.persona) | sort | join(",")' 'competing-team-lead,executive-sponsor,test-lead'
```

3. Relabel the existing bonus block:
```bash
# Bonus validation: --cap-usd non-numeric → exit 2 (D-58 no-sentinel rule)
```
(Remove the "Case 6" label from it.)

This ensures D-04/D-05's "6th scenario" language matches the actual Case 6 in the script.
  </action>
  <verify>
    <automated>bash scripts/test-budget-cap.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "9bench-all-triggered" scripts/test-budget-cap.sh` returns at least 5 (scenario name appears in run_case call + assert calls)
    - `grep "budget-classifier-9bench-all.json" scripts/test-budget-cap.sh` finds the fixture reference
    - `grep "6 cap scenarios" scripts/test-budget-cap.sh` finds the updated header
    - The bonus non-numeric check no longer says "Case 6"
    - `bash scripts/test-budget-cap.sh` exits 0 with "All budget-cap scenarios passed."
    - Output includes `PASS: 9bench-all-triggered: SPAWN_BENCH=security-reviewer,compliance-reviewer,dual-deploy-reviewer,performance-reviewer,finops-auditor,air-gap-reviewer`
  </acceptance_criteria>
  <done>9-bench budget-cap scenario passes as Case 6 in test-budget-cap.sh (per D-04, D-05); top-6 selection by priority confirmed; 3 personas skipped with budget_cap reason; bonus non-numeric check preserved but unlabeled as a case number</done>
</task>

<task type="auto">
  <name>Task 3: Extend test-blinded-reader.sh with LLM-as-judge evaluation</name>
  <read_first>
    - scripts/test-blinded-reader.sh
    - tests/fixtures/blinded-reader/multi-signal-fixture.md
    - persona-metadata/security-reviewer.yml
    - persona-metadata/compliance-reviewer.yml
  </read_first>
  <files>scripts/test-blinded-reader.sh</files>
  <action>
Extend `scripts/test-blinded-reader.sh` to add a second mode: `--live-judge` that performs the actual LLM-as-judge persona attribution evaluation (the Phase 7 live run referenced in the script's existing comments).

Keep all existing structural readiness checks intact. Add the following after the existing checks:

1. A `--live-judge` flag check at script start. If the flag is NOT passed, skip the LLM-as-judge section entirely (existing behavior preserved for CI structural checks).

2. When `--live-judge` is passed, the script:
   a. Looks for a `.council/` run directory containing 6+ persona scorecards (the most recent run, or `--run-dir=<path>` override)
   b. For each persona scorecard found:
      - Strips the filename (anonymizes — evaluator should not see which persona wrote it)
      - Extracts the scorecard body text
      - Builds a judge prompt: "Given the following anonymized review scorecard, identify which persona (from the list: [all 9 bench persona names + 4 core persona names]) most likely wrote this review. Consider: voice register, types of concerns raised, banned-phrase absence, characteristic objection patterns. Reply with ONLY the persona slug."
      - Invokes `claude --print --dangerously-skip-permissions` with the judge prompt (or documents that this requires manual invocation if claude CLI is not available)
   c. Compares each judge attribution to the actual persona (from filename)
   d. Computes accuracy: `correct / total`
   e. Asserts accuracy >= 0.80 (80% per PQUAL-03 / REL-02)
   f. Prints a summary table: `persona | judge_guess | correct?`

3. The `--live-judge` section should be clearly marked:
```bash
# === LIVE LLM-AS-JUDGE EVALUATION (Phase 7 REL-02 / PQUAL-03) ===
# Requires: a completed .council/ run with 6+ persona scorecards
# Invocation: ./scripts/test-blinded-reader.sh --live-judge [--run-dir=<path>]
# NOT run in CI — requires Claude CLI auth + a real review run
```

4. Exit code: If `--live-judge` is passed and accuracy < 80%, exit 1 with a clear failure message showing the attribution table.

Do NOT modify the existing structural checks or their exit logic. The `--live-judge` mode is additive and runs AFTER the structural checks pass.
  </action>
  <verify>
    <automated>bash scripts/test-blinded-reader.sh && grep -q "live-judge" scripts/test-blinded-reader.sh && grep -q "PQUAL-03" scripts/test-blinded-reader.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/test-blinded-reader.sh` (without --live-judge) still passes with existing structural checks
    - `grep -c "live-judge" scripts/test-blinded-reader.sh` returns >= 3 (flag parse + section header + invocation docs)
    - `grep "accuracy.*0.80\|80%" scripts/test-blinded-reader.sh` finds the threshold assertion
    - `grep "PQUAL-03\|REL-02" scripts/test-blinded-reader.sh` finds requirement traceability
    - Script contains persona list for the judge prompt (all 13 persona names)
  </acceptance_criteria>
  <done>Blinded-reader script extended with --live-judge mode for LLM-as-judge persona attribution; structural checks still pass in CI; live evaluation ready for manual UAT run</done>
</task>

<task type="auto">
  <name>Task 4: Execute live /devils-council:review on UAT fixture and validate synthesis</name>
  <read_first>
    - tests/fixtures/uat-9bench/anaconda-platform-chart.md
    - bin/dc-validate-synthesis.sh
    - commands/review.md
  </read_first>
  <files></files>
  <action>
Execute a live `/devils-council:review` run against the UAT fixture created in Task 1. This task produces NO new files — it exercises the full review pipeline and validates the output. The `<files>` element is intentionally empty because this task performs only runtime invocation and validation of existing infrastructure.

Steps:

1. Run the full review against the UAT fixture:
   ```bash
   /devils-council:review tests/fixtures/uat-9bench/anaconda-platform-chart.md --type=plan
   ```
   (The `--type=plan` flag ensures exec_keyword fires for the executive-sponsor persona.)

   If `/devils-council:review` is not available in the execution context (e.g., running as a subagent without plugin access), use the equivalent CLI invocation:
   ```bash
   claude --plugin-dir . -p "/devils-council:review tests/fixtures/uat-9bench/anaconda-platform-chart.md --type=plan" --dangerously-skip-permissions
   ```

2. After the run completes, identify the run directory:
   ```bash
   LATEST_RUN=$(ls -td .council/*/ 2>/dev/null | head -1)
   ```

3. Validate the synthesis output:
   ```bash
   bash bin/dc-validate-synthesis.sh "${LATEST_RUN}SYNTHESIS.md"
   ```
   This MUST exit 0 (Chair synthesis has <= 3 Top-3 blockers per REL-01).

4. Count persona scorecards in the run directory:
   ```bash
   SCORECARD_COUNT=$(find "$LATEST_RUN" -name "*.md" ! -name "SYNTHESIS.md" ! -name "MANIFEST*" | wc -l)
   ```
   Assert at least 6 scorecards exist (4 core + at least 2 bench that fit under budget; ideally 10 = 4 core + 6 bench under cap).

5. Verify MANIFEST shows budget enforcement:
   ```bash
   jq -e '.budget.over_budget == true' "${LATEST_RUN}MANIFEST.json"
   jq -e '.personas_skipped | length >= 3' "${LATEST_RUN}MANIFEST.json"
   ```

If the live review run cannot complete (e.g., Claude CLI auth not available, plugin not loadable in subagent context), document the exact commands needed for manual execution and mark as requiring human verification. The critical assertion is that `bin/dc-validate-synthesis.sh` passes on the output.
  </action>
  <verify>
    <automated>LATEST_RUN=$(ls -td .council/*/ 2>/dev/null | head -1) && test -n "$LATEST_RUN" && bash bin/dc-validate-synthesis.sh "${LATEST_RUN}SYNTHESIS.md" && test $(find "$LATEST_RUN" -name "*.md" ! -name "SYNTHESIS.md" ! -name "MANIFEST*" | wc -l) -ge 6</automated>
  </verify>
  <acceptance_criteria>
    - A `.council/<run>/` directory exists with a completed review run
    - `bin/dc-validate-synthesis.sh .council/<run>/SYNTHESIS.md` exits 0 (REL-01: Chair synthesis <= 3 Top-3 blockers)
    - At least 6 persona scorecards exist in the run directory (4 core + bench under budget)
    - `jq '.budget.over_budget' .council/<run>/MANIFEST.json` returns `true` (9 triggered > 6 max)
    - `jq '.personas_skipped | length' .council/<run>/MANIFEST.json` returns >= 3
  </acceptance_criteria>
  <done>Live /devils-council:review run completed on UAT fixture; Chair synthesis validated by dc-validate-synthesis.sh; budget cap enforced correctly at 9-bench scale; at least 6 persona scorecards produced</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI test harness -> fixture files | Test fixtures are static JSON/Markdown; no untrusted input crosses |
| LLM-as-judge -> Claude CLI | --live-judge invokes Claude CLI with crafted prompts containing scorecard text |
| Live review run -> .council/ output | Full pipeline execution writes to .council/; output validated by dc-validate-synthesis.sh |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Spoofing | LLM-as-judge prompt | accept | Judge is an evaluation tool, not a security boundary; scorecard content is already validated by dc-validate-scorecard.sh before reaching the judge |
| T-07-02 | Information Disclosure | test fixture content | accept | Fixture is synthetic (per D-02), contains no proprietary content; committed to public repo intentionally |
| T-07-03 | Tampering | budget-classifier-9bench-all.json | mitigate | Fixture is version-controlled; test-budget-cap.sh validates exact expected outputs — any tampering breaks the test assertions |
| T-07-04 | Tampering | .council/ live run output | mitigate | dc-validate-synthesis.sh validates structural correctness; MANIFEST.json schema enforced by dc-budget-plan.sh |
</threat_model>

<verification>
1. `bash scripts/test-budget-cap.sh` exits 0 with all 6 scenarios + bonus validation passing
2. `bash scripts/test-blinded-reader.sh` exits 0 (structural readiness)
3. `jq '.triggered_personas | length' tests/fixtures/bench-personas/budget-classifier-9bench-all.json` returns 9
4. `tests/fixtures/uat-9bench/anaconda-platform-chart.md` exists with 80+ lines containing all signal-triggering patterns
5. `bash bin/dc-validate-synthesis.sh .council/<latest-run>/SYNTHESIS.md` exits 0 (live review synthesis validated)
</verification>

<success_criteria>
- 9-bench budget-cap scenario in test-budget-cap.sh passes as Case 6 (per D-04, D-05) with correct top-6 selection and 3 personas skipped
- Blinded-reader script has --live-judge capability for Phase 7 UAT manual run
- UAT fixture is committed and reproducible in CI
- Live /devils-council:review run completed and synthesis validated by dc-validate-synthesis.sh (REL-01)
- All existing tests continue to pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/07-integration-uat-release/07-01-SUMMARY.md`
</output>
