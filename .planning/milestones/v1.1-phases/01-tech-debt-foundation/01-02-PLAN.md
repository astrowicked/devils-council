---
phase: 01-tech-debt-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
batch: 2
files_modified:
  - agents/README.md
  - agents/AUTHORING.md
  - scripts/validate-personas.sh
autonomous: true
requirements: [TD-06]
commit_message: "refactor(agents): rename README.md to AUTHORING.md (TD-06)"
must_haves:
  truths:
    - "`agents/AUTHORING.md` exists at git HEAD; `agents/README.md` does not"
    - "`scripts/validate-personas.sh` excludes `AUTHORING.md` (not `README.md`) from persona-validation iteration"
    - "`grep -rn 'agents/README'` across .planning/ agents/ scripts/ .github/ hooks/ (md/sh/yml/json/yaml) returns zero matches"
    - "`claude plugin validate .` exits 0 post-rename (Claude Code plugin loader no longer mis-classifies an `agents/*.md` authoring doc as a subagent)"
    - "`bash scripts/validate-personas.sh` exits 0 post-rename across all current personas (including AUTHORING.md being skipped, not validated as a persona)"
  artifacts:
    - path: "agents/AUTHORING.md"
      provides: "Persona-authoring guide (body unchanged from old agents/README.md)"
      contains: "Persona Authoring"
    - path: "scripts/validate-personas.sh"
      provides: "Skip-case updated to exclude AUTHORING.md"
      contains: "AUTHORING.md|.gitkeep"
  key_links:
    - from: "scripts/validate-personas.sh line ~638"
      to: "agents/AUTHORING.md"
      via: "case statement excluding basename AUTHORING.md from persona validation iteration"
      pattern: "AUTHORING\\.md\\|\\.gitkeep"
    - from: "git tracking"
      to: "file rename (not delete+add)"
      via: "git mv"
      pattern: "rename from agents/README.md"
---

<objective>
Execute the `agents/README.md` → `agents/AUTHORING.md` rename (TD-06) atomically: file rename via `git mv`, one-line validator edit, full reference sweep, post-rename validation gates. Matches CONTEXT.md D-09/D-10 and PITFALLS.md P-10 (the reference sweep is the step people skip; make it explicit).

Purpose: Eliminate Claude Code plugin-loader mis-classification of an `agents/*.md` authoring doc as a subagent. Plugin loader registers every `agents/*.md` at install time; in v1.0 this was papered over by a validator-script skip, but the loader still attempts the registration. Rename moves the authoring doc out of the persona-registration blast radius BEFORE Phase 4 adds 6 new persona files (P-10 prevention: loader mis-classification grows 1 → 7 if we ship new personas before the rename).

Output:
- `agents/AUTHORING.md` with content identical to the old `agents/README.md` (header changed only if the H1 says "Persona Authoring" already; it does — no body edit needed)
- `agents/README.md` removed (via `git mv`, preserving history)
- `scripts/validate-personas.sh` line 638 case-statement updated
- Reference-sweep assertion confirming zero stale `agents/README` references
- `claude plugin validate .` and `bash scripts/validate-personas.sh` both green
- Single commit: `refactor(agents): rename README.md to AUTHORING.md (TD-06)`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-tech-debt-foundation/01-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@agents/README.md
@scripts/validate-personas.sh
</context>

<interfaces>
<!-- Pre-rename state of the load-bearing line in validate-personas.sh — executor must edit this EXACT string -->

`scripts/validate-personas.sh` line 638 (current state, pre-edit):
```bash
        README.md|.gitkeep) continue ;;
```

Target state (post-edit):
```bash
        AUTHORING.md|.gitkeep) continue ;;
```

`agents/README.md` current H1 (first line of body):
```markdown
# Persona Authoring
```

The H1 is already `Persona Authoring` — body requires zero edits. Only the filename changes.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Pre-rename baseline — capture current validator green state</name>
  <files>(no file edits — read-only baseline)</files>
  <read_first>
    - scripts/validate-personas.sh (confirm line 638 still says `README.md|.gitkeep) continue ;;` exactly; if line number has shifted, grep for the exact string to locate)
  </read_first>
  <action>
Run the following three commands and record their output in Plan 01-02 SUMMARY.md:

1. `grep -n "README.md|.gitkeep" scripts/validate-personas.sh` — must output one line referencing line 638 (or wherever the case statement lives). Save the exact line number seen; subsequent edit targets that line.
2. `bash scripts/validate-personas.sh; echo "exit=$?"` — must print `exit=0`. This establishes the pre-rename baseline is green; if it's not green BEFORE the rename, stop and report (TD-06 cannot proceed against a broken baseline).
3. `claude plugin validate . 2>&1 | tail -5; echo "exit=$?"` — must print `exit=0` with a success-indicator line in output. Same rationale.

If any command fails, halt the plan and report. All three must succeed before Task 2 runs.
  </action>
  <verify>
    <automated>bash scripts/validate-personas.sh &amp;&amp; grep -q "README.md|.gitkeep" scripts/validate-personas.sh</automated>
  </verify>
  <done>Baseline captured; `validate-personas.sh` exits 0; current case-statement line identified with its line number; no rename has occurred yet.</done>
</task>

<task type="auto">
  <name>Task 2: Rename file + update validator in one step</name>
  <files>
    agents/README.md
    agents/AUTHORING.md
    scripts/validate-personas.sh
  </files>
  <read_first>
    - agents/README.md (confirm body starts with `# Persona Authoring` — if not, STOP and report; the rename rationale assumes the doc is already titled authoring, not readme)
    - scripts/validate-personas.sh around line 638 (the case-statement pattern confirmed in Task 1)
  </read_first>
  <action>
Perform the rename + validator edit as two deterministic operations:

1. `git mv agents/README.md agents/AUTHORING.md` — git-aware rename preserves history. Do NOT use `mv` or `rm + Write` — those would lose git blame continuity. After the command, `agents/AUTHORING.md` must exist and `agents/README.md` must not exist.

2. Edit `scripts/validate-personas.sh`: change the exact substring `README.md|.gitkeep` to `AUTHORING.md|.gitkeep` in the case statement that excludes non-persona files. Use `sed -i` (GNU) or `sed -i ''` (BSD/macOS) with a precise pattern, OR use the Edit tool with the exact line content. Do NOT do a global sed — only ONE occurrence of this exact case pattern should exist in the file; verify with `grep -c "README.md|.gitkeep" scripts/validate-personas.sh` (expect 1 BEFORE edit, 0 AFTER edit) and `grep -c "AUTHORING.md|.gitkeep" scripts/validate-personas.sh` (expect 0 BEFORE, 1 AFTER).

Do NOT edit the body of the (newly named) `agents/AUTHORING.md` — its H1 is already "Persona Authoring" and references nothing by its own filename. Content preservation is part of the contract.
  </action>
  <verify>
    <automated>test -f agents/AUTHORING.md &amp;&amp; test ! -f agents/README.md &amp;&amp; grep -c "AUTHORING.md|.gitkeep" scripts/validate-personas.sh | grep -qx "1" &amp;&amp; ! grep -q "README.md|.gitkeep" scripts/validate-personas.sh</automated>
  </verify>
  <done>AUTHORING.md exists; README.md does not; validator case statement uses AUTHORING.md; zero lingering `README.md|.gitkeep` references.</done>
</task>

<task type="auto">
  <name>Task 3: Reference sweep — P-10 prevention</name>
  <files>(no file edits unless sweep finds stale references)</files>
  <read_first>
    - .planning/research/PITFALLS.md §P-10 (this sweep is load-bearing)
    - .planning/phases/01-tech-debt-foundation/01-CONTEXT.md §TD-06 D-09 (grep command spec)
  </read_first>
  <action>
Run the full reference-sweep command EXACTLY as specified in the context decision:

```bash
grep -rn "agents/README" .planning/ agents/ scripts/ .github/ hooks/ \
  --include="*.md" --include="*.sh" --include="*.yml" --include="*.yaml" --include="*.json" \
  2>/dev/null
```

**Expected result: zero lines of output** (exit code 1 from grep means "no matches" which is the success state here).

If the command returns ANY matches, the rename is incomplete. For each match:
1. Inspect the file and line.
2. If the reference is a LIVE link or path that needs to work (e.g., a README link, a code comment pointing at the file path), update it to `agents/AUTHORING.md`.
3. If the reference is a HISTORICAL record (e.g., a v1.0 SUMMARY.md that mentions `agents/README.md` as it existed at the time), LEAVE IT as historical record — but move it to the exclusion list by adjusting the grep scope. However, per CONTEXT.md D-09, initial grep confirmed ZERO external refs, so realistically no hits are expected.
4. After fixing any hits, re-run the grep and confirm zero output.

Also verify the Contributing section of README.md (root, not agents/) does not refer to a now-moved path. Run:
```bash
grep -n "agents/README\|agents/AUTHORING" README.md
```
If README.md references `agents/` authoring docs, update any mention to `agents/AUTHORING.md`. (The public README's Contributing section says "Personas are markdown files under `agents/`" — that language is unaffected by this rename.)

Record the final grep command output in Plan 01-02 SUMMARY.md as evidence: `grep -rn "agents/README" ... returned: <exit code + line count>`.
  </action>
  <verify>
    <automated>! grep -rn "agents/README" .planning/ agents/ scripts/ .github/ hooks/ --include="*.md" --include="*.sh" --include="*.yml" --include="*.yaml" --include="*.json" 2&gt;/dev/null</automated>
  </verify>
  <done>The full-repo reference sweep returns zero matches. Any hits found were updated to `agents/AUTHORING.md` and the sweep re-run confirmed clean. Evidence recorded in SUMMARY.md.</done>
</task>

<task type="auto">
  <name>Task 4: Post-rename validation gates</name>
  <files>(no file edits — validation only)</files>
  <read_first>
    - .planning/phases/01-tech-debt-foundation/01-CONTEXT.md §TD-06 D-10 (both validators must still exit 0)
    - agents/ directory listing (confirm AUTHORING.md is the only non-persona .md left)
  </read_first>
  <action>
Run both validation gates required by D-10. Both MUST exit 0; if either fails, the rename is broken and Task 2 must be revisited.

1. `claude plugin validate .` — must exit 0 and include a success token in output. This confirms the Claude Code plugin loader no longer sees a malformed-subagent `README.md` in `agents/`. Expected behavior: the loader may still surface AUTHORING.md if it pattern-matches `agents/*.md`, but it won't fail validation because (a) the body looks like arbitrary markdown, not a malformed subagent, and (b) the loader is tolerant of missing subagent frontmatter on documentation-looking files. If `claude plugin validate` reports an error citing AUTHORING.md specifically, the rename is wrong — capture the exact error and stop.

2. `bash scripts/validate-personas.sh` — must exit 0. The case-statement edit from Task 2 means AUTHORING.md is SKIPPED from persona validation (not validated as a persona with missing frontmatter, which would fail). Run and confirm exit 0.

3. Additional sanity: `ls agents/*.md | grep -c AUTHORING.md` must equal 1; `ls agents/*.md | grep -c README.md` must equal 0.

4. Optional but recommended: inspect the output of `bash scripts/validate-personas.sh` (stdout/stderr) and confirm it does NOT mention AUTHORING.md as a validated persona. AUTHORING.md should be silent-skipped. If the validator's output log mentions AUTHORING.md, the skip case didn't match — re-verify Task 2 edit landed correctly.
  </action>
  <verify>
    <automated>bash scripts/validate-personas.sh &amp;&amp; claude plugin validate . &amp;&amp; ls agents/*.md | grep -c AUTHORING.md | grep -qx "1" &amp;&amp; ! ls agents/*.md 2&gt;/dev/null | grep -q "README.md"</automated>
  </verify>
  <done>Both validators exit 0; AUTHORING.md present exactly once; README.md absent from agents/.</done>
</task>

<task type="auto">
  <name>Task 5: Commit the rename</name>
  <files>
    agents/AUTHORING.md
    scripts/validate-personas.sh
  </files>
  <read_first>
    - CLAUDE.md global: "Never add Co-Authored-By lines to git commits" — project rule
    - .planning/phases/01-tech-debt-foundation/01-CONTEXT.md §D-15 (Batches 2/3 ship as per-TD commits)
  </read_first>
  <action>
1. Confirm staged files:
   ```bash
   git status --short
   ```
   Expected output (exact):
   ```
   R  agents/README.md -> agents/AUTHORING.md
   M  scripts/validate-personas.sh
   ```
   (`git mv` from Task 2 already staged the rename; only the sh edit needs explicit `git add`.)

2. Stage the validator edit:
   ```bash
   git add scripts/validate-personas.sh
   ```

3. Confirm the full staged set:
   ```bash
   git diff --cached --name-status
   ```
   Expected lines (order may vary):
   ```
   R100  agents/README.md    agents/AUTHORING.md
   M     scripts/validate-personas.sh
   ```
   The `R100` similarity score confirms git recognizes this as a rename (100% similarity = content unchanged).

4. Commit with the exact Batch-2 message:
   ```bash
   git commit -m "refactor(agents): rename README.md to AUTHORING.md (TD-06)"
   ```
   Do NOT add `Co-Authored-By`. Do NOT include any other files.

5. Verify:
   ```bash
   git log -1 --format=%s
   # expected: refactor(agents): rename README.md to AUTHORING.md (TD-06)
   git log -1 --format=%B | grep -c "Co-Authored-By"
   # expected: 0
   ```
  </action>
  <verify>
    <automated>git log -1 --format=%s | grep -qx "refactor(agents): rename README.md to AUTHORING.md (TD-06)" &amp;&amp; [ "$(git log -1 --format=%B | grep -c 'Co-Authored-By')" = "0" ] &amp;&amp; git show --stat HEAD | grep -qE "agents/(README\.md|AUTHORING\.md)"</automated>
  </verify>
  <done>Commit subject matches exactly; zero Co-Authored-By trailers; commit shows both the renamed file and the validator edit.</done>
</task>

</tasks>

<verification>
Overall TD-06 success criteria:

1. `test -f agents/AUTHORING.md` exits 0
2. `test ! -f agents/README.md` exits 0
3. `grep -q "AUTHORING.md|.gitkeep" scripts/validate-personas.sh` exits 0
4. `! grep -q "README.md|.gitkeep" scripts/validate-personas.sh` exits 0
5. `! grep -rn "agents/README" .planning/ agents/ scripts/ .github/ hooks/ --include='*.md' --include='*.sh' --include='*.yml' --include='*.yaml' --include='*.json' 2>/dev/null` exits 0 (no matches)
6. `bash scripts/validate-personas.sh` exits 0
7. `claude plugin validate .` exits 0
8. `git log -1 --format=%s` equals `refactor(agents): rename README.md to AUTHORING.md (TD-06)`
9. `git log -1 --format=%B | grep -c "Co-Authored-By"` equals 0
10. `git show HEAD --stat` confirms rename is recorded as a rename, not delete+add
</verification>

<success_criteria>
- File rename recorded as `R` by git (history preserved)
- Validator script updated atomically in the same commit
- Zero stale `agents/README` references anywhere in tracked source tree
- Both `claude plugin validate` and `validate-personas.sh` remain green post-rename
- P-10 prevention: reference sweep explicitly verified on Phase 1 merge day (not assumed from prior grep run)
- TD-06 shipped before Phase 4 adds 6 new persona files (dependency constraint per ROADMAP.md line 58)
</success_criteria>

<output>
After completion, create `.planning/phases/01-tech-debt-foundation/01-02-SUMMARY.md` documenting:
- Exact commit hash for `refactor(agents): rename README.md to AUTHORING.md (TD-06)`
- Line number of the validator case-statement (pre-edit and post-edit; confirm identical, just the pattern token changed)
- Full output of the reference-sweep grep command (the ZERO-result confirmation)
- Output of `git show HEAD --stat` proving the rename is git-aware (R100)
- Any deviation from D-09/D-10 with rationale (none expected)
</output>
