---
phase: 04-six-personas-atomic-conductor-wiring
plan: 07
type: execute
wave: 2
depends_on: [04-01, 04-02, 04-03, 04-04, 04-05, 04-06]
files_modified:
  - commands/review.md
  - bin/dc-budget-plan.sh
autonomous: true
requirements: [BNCH2-01, BNCH2-02, BNCH2-03, BNCH2-04, BNCH2-05, CORE-EXT-01]

must_haves:
  truths:
    - "Bench whitelist in commands/review.md expanded from 4 to 9 entries in a single atomic commit per D-13"
    - "always_invoke_on field in Junior Engineer sidecar is read by the conductor and JE is auto-appended to spawn list on code-diff artifacts per D-05"
    - "Junior Engineer bypasses the bench budget cap entirely per D-06"
    - "Core cardinality stays at 4 — grep -c '^tier: core$' persona-metadata/*.yml returns 4"
    - "Display-name map has 6 new entries for all new personas"
    - "validate-personas.sh passes across all 13 personas (4 core + 9 bench + Chair + classifier) per D-14"
  artifacts:
    - path: "commands/review.md"
      provides: "Conductor with 9-bench whitelist + display names"
      contains: "compliance-reviewer"
    - path: "bin/dc-budget-plan.sh"
      provides: "Budget plan with always_invoke_on reading"
      contains: "always_invoke_on"
  key_links:
    - from: "commands/review.md"
      to: "agents/compliance-reviewer.md"
      via: "bench persona enumeration"
      pattern: "compliance-reviewer"
    - from: "commands/review.md"
      to: "agents/junior-engineer.md"
      via: "bench persona enumeration"
      pattern: "junior-engineer"
    - from: "bin/dc-budget-plan.sh"
      to: "persona-metadata/junior-engineer.yml"
      via: "always_invoke_on field reading"
      pattern: "always_invoke_on"
---

<objective>
Wire all 6 new personas into the conductor as a single atomic commit. This is the Wave 2 plan that lands AFTER all 6 persona files exist from Wave 1. Three conductor surfaces change: (1) bench persona enumeration in commands/review.md expands from 4 to 9, (2) display-name map gains 6 entries, (3) bin/dc-budget-plan.sh reads the new always_invoke_on sidecar field and auto-appends Junior Engineer outside the budget cap.

Purpose: Per D-07 and D-13, all conductor changes land atomically. No intermediate state where the whitelist references personas that don't exist, and no incremental whitelist growth.
Output: commands/review.md + bin/dc-budget-plan.sh updated; all 13 personas pass validate-personas.sh
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-six-personas-atomic-conductor-wiring/04-CONTEXT.md
@.planning/phases/04-six-personas-atomic-conductor-wiring/04-RESEARCH.md

@commands/review.md
@bin/dc-budget-plan.sh
@config.json
@persona-metadata/junior-engineer.yml

<interfaces>
<!-- commands/review.md conductor surfaces to modify: -->
<!-- Line ~155: Haiku whitelist — ALREADY at 8 entries from Phase 3. No change needed. -->
<!-- Lines ~319-323: Bench persona enumeration — currently 4 entries (security-reviewer, finops-auditor, air-gap-reviewer, dual-deploy-reviewer). Add 5 new: compliance-reviewer, performance-reviewer, test-lead, executive-sponsor, competing-team-lead. -->
<!-- Lines ~382-384: Validator loop canonical order — keep core + BENCH_SPAWN_LIST pattern unchanged. -->
<!-- Lines ~797-807: Display-name map — currently 8 entries. Add 6 new. -->
<!-- Line ~304: Bench fan-out comment — update "Phase 6" reference to "Phase 4/6". -->

<!-- bin/dc-budget-plan.sh: -->
<!-- Line 72: PRIORITY_ORDER_JSON fallback — keep old 4-entry fallback for backward compat; primary is 9-entry from config.json -->
<!-- New feature: read always_invoke_on from sidecar files, auto-append to spawn list BEFORE budget cap is applied -->
<!-- JE must NOT count against budget cap per D-06 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend bin/dc-budget-plan.sh with always_invoke_on reading</name>
  <files>bin/dc-budget-plan.sh</files>
  <read_first>
    - bin/dc-budget-plan.sh (full file — understand current flow: triggered_personas from MANIFEST, priority ordering, budget cap, spawn list output)
    - persona-metadata/junior-engineer.yml (the sidecar with always_invoke_on: [code-diff])
    - config.json (budget.bench_priority_order — 9-entry array)
    - commands/review.md lines 295-340 (conductor's bench fan-out — understand how SPAWN_BENCH is consumed)
  </read_first>
  <action>
Modify `bin/dc-budget-plan.sh` to implement the always_invoke_on bypass per D-05 and D-06. The change adds a new block AFTER the existing triggered-persona extraction (line ~80) and BEFORE the priority ordering (line ~111). Specific changes:

1. **Add always_invoke_on scanning block** (insert after line 80, before the --only filter block at line ~84):

```bash
# --- always_invoke_on bypass (D-05, D-06) ---
# Scan persona-metadata/*.yml for always_invoke_on fields matching the artifact_type.
# These personas bypass both the classifier and the budget cap — they are auto-appended
# to SPAWN_BENCH unconditionally and are NOT counted against MAX_SPAWNABLE.
ARTIFACT_TYPE=$(jq -r '.artifact_type // ""' "$MANIFEST")
ALWAYS_INVOKE_JSON='[]'
if [ -n "$ARTIFACT_TYPE" ]; then
  PERSONA_DIR="${REPO_ROOT}/persona-metadata"
  if [ -d "$PERSONA_DIR" ]; then
    for yml in "$PERSONA_DIR"/*.yml; do
      [ -f "$yml" ] || continue
      SLUG=$(basename "$yml" .yml)
      # Check if this sidecar has always_invoke_on containing the current artifact_type
      if yq -e ".always_invoke_on // [] | .[] | select(. == \"$ARTIFACT_TYPE\")" "$yml" >/dev/null 2>&1; then
        ALWAYS_INVOKE_JSON=$(printf '%s' "$ALWAYS_INVOKE_JSON" | jq --arg s "$SLUG" '. + [$s]')
      fi
    done
  fi
fi
```

2. **Modify the stdout output** (at the end, after SPAWN_CSV is computed around line ~134): Append always_invoke_on personas to SPAWN_CSV so they appear in the spawn list but are tracked separately:

```bash
# Append always_invoke_on personas to spawn list (outside budget cap)
ALWAYS_CSV=$(printf '%s' "$ALWAYS_INVOKE_JSON" | jq -r 'join(",")')
if [ -n "$ALWAYS_CSV" ] && [ "$ALWAYS_CSV" != "" ]; then
  if [ -n "$SPAWN_CSV" ]; then
    SPAWN_CSV="${SPAWN_CSV},${ALWAYS_CSV}"
  else
    SPAWN_CSV="$ALWAYS_CSV"
  fi
fi
```

3. **Add always_invoke_on to MANIFEST** (in the jq merge block around line ~154): Add `.always_invoked` array to the budget block:

```bash
jq --argjson always "$ALWAYS_INVOKE_JSON" \
   ... existing args ... '
  .budget = {
    ... existing fields ...,
    always_invoked: $always
  }
  ... rest unchanged ...
' "$MANIFEST" > "$TMP_MF" && mv "$TMP_MF" "$MANIFEST"
```

4. **Keep the 4-entry fallback** in PRIORITY_ORDER_JSON (line 72) unchanged for backward compatibility — the primary 9-entry array comes from config.json.

The always_invoke_on personas:
- Are NOT included in CANDIDATE_JSON (they bypass priority ordering)
- Are NOT counted against MAX_SPAWNABLE (they bypass budget cap per D-06)
- ARE included in the final SPAWN_BENCH output (conductor spawns them)
- ARE tracked in MANIFEST.budget.always_invoked for observability
  </action>
  <verify>
    <automated>bash -n bin/dc-budget-plan.sh && echo "syntax ok"; grep -c 'always_invoke_on' bin/dc-budget-plan.sh; grep -c 'ALWAYS_INVOKE' bin/dc-budget-plan.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n bin/dc-budget-plan.sh` exits 0 (valid syntax)
    - `grep -c 'always_invoke_on' bin/dc-budget-plan.sh` returns >= 2 (comment + code)
    - `grep -c 'ALWAYS_INVOKE' bin/dc-budget-plan.sh` returns >= 3 (variable declarations + usage)
    - `grep -c 'ARTIFACT_TYPE' bin/dc-budget-plan.sh` returns >= 2
    - `grep 'always_invoked' bin/dc-budget-plan.sh` shows the MANIFEST field
    - The existing PRIORITY_ORDER_JSON fallback on line 72 is unchanged (still 4-entry)
  </acceptance_criteria>
  <done>dc-budget-plan.sh reads always_invoke_on from persona sidecars, auto-appends matching personas to spawn list outside budget cap, tracks in MANIFEST.budget.always_invoked</done>
</task>

<task type="auto">
  <name>Task 2: Expand bench whitelist and display-name map in commands/review.md</name>
  <files>commands/review.md</files>
  <read_first>
    - commands/review.md lines 295-340 (bench fan-out section — current 4-entry bench list)
    - commands/review.md lines 796-808 (display-name map — current 8 entries)
    - commands/review.md lines 145-168 (Haiku whitelist — confirm already 8 entries, no change needed)
    - commands/review.md lines 380-395 (validator loop canonical order — confirm BENCH_SPAWN_LIST pattern)
  </read_first>
  <action>
Modify `commands/review.md` in three locations. All changes in a single commit per D-13:

**1. Bench persona enumeration (lines ~319-323):** Replace the current 4-entry list with the full 9-entry list:

```markdown
Bench personas supported in v1.1:
- `security-reviewer` -> writes `<RUN_DIR>/security-reviewer-draft.md`
- `finops-auditor` -> writes `<RUN_DIR>/finops-auditor-draft.md`
- `air-gap-reviewer` -> writes `<RUN_DIR>/air-gap-reviewer-draft.md`
- `dual-deploy-reviewer` -> writes `<RUN_DIR>/dual-deploy-reviewer-draft.md`
- `compliance-reviewer` -> writes `<RUN_DIR>/compliance-reviewer-draft.md`
- `performance-reviewer` -> writes `<RUN_DIR>/performance-reviewer-draft.md`
- `test-lead` -> writes `<RUN_DIR>/test-lead-draft.md`
- `executive-sponsor` -> writes `<RUN_DIR>/executive-sponsor-draft.md`
- `competing-team-lead` -> writes `<RUN_DIR>/competing-team-lead-draft.md`
```

Also add a note about Junior Engineer's always_invoke_on bypass:
```markdown
Additionally, `junior-engineer` is auto-appended to BENCH_SPAWN_LIST when
`artifact_type` is `code-diff` (via the `always_invoke_on` field in its
sidecar, read by `bin/dc-budget-plan.sh`). Junior Engineer bypasses the
budget cap and does not count against MAX_SPAWNABLE.
```

**2. Display-name map (lines ~797-807):** Add 6 new entries after the existing 8:

```markdown
- `compliance-reviewer` -> `Compliance Reviewer`
- `performance-reviewer` -> `Performance Reviewer`
- `test-lead` -> `Test Lead`
- `executive-sponsor` -> `Executive Sponsor`
- `competing-team-lead` -> `Competing Team Lead`
- `junior-engineer` -> `Junior Engineer`
```

**3. Bench fan-out comment (line ~304):** Update "Phase 6 bench fan-out" to "Phase 4/6 bench fan-out" to reflect the expanded roster.

Do NOT change:
- Haiku whitelist (line ~155) — already at 8 entries from Phase 3
- Core spawn list (lines ~299-302) — stays at 4 (staff-engineer, sre, product-manager, devils-advocate)
- Validator loop canonical order (lines ~382-384) — keep `[staff-engineer, sre, product-manager, devils-advocate]` + BENCH_SPAWN_LIST pattern
  </action>
  <verify>
    <automated>grep -c 'compliance-reviewer\|performance-reviewer\|test-lead\|executive-sponsor\|competing-team-lead\|junior-engineer' commands/review.md; grep -c 'Compliance Reviewer\|Performance Reviewer\|Test Lead\|Executive Sponsor\|Competing Team Lead\|Junior Engineer' commands/review.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'compliance-reviewer' commands/review.md` returns >= 2 (bench list + display map)
    - `grep -c 'performance-reviewer' commands/review.md` returns >= 2
    - `grep -c 'test-lead' commands/review.md` returns >= 2
    - `grep -c 'executive-sponsor' commands/review.md` returns >= 2
    - `grep -c 'competing-team-lead' commands/review.md` returns >= 2
    - `grep -c 'junior-engineer' commands/review.md` returns >= 2
    - `grep -c 'Compliance Reviewer' commands/review.md` returns >= 1 (display name)
    - `grep -c 'Junior Engineer' commands/review.md` returns >= 1
    - `grep -c 'always_invoke_on' commands/review.md` returns >= 1 (documentation of JE bypass)
    - Core spawn list unchanged: grep for staff-engineer, sre, product-manager, devils-advocate at lines ~299-302
  </acceptance_criteria>
  <done>commands/review.md bench whitelist expanded from 4 to 9, display-name map has 14 entries (8 existing + 6 new), JE always_invoke_on documented, core cardinality unchanged at 4</done>
</task>

<task type="auto">
  <name>Task 3: Full validation + core cardinality assertion</name>
  <files></files>
  <read_first>
    - persona-metadata/staff-engineer.yml (verify tier: core)
    - persona-metadata/sre.yml (verify tier: core)
    - persona-metadata/product-manager.yml (verify tier: core)
    - persona-metadata/devils-advocate.yml (verify tier: core)
    - persona-metadata/junior-engineer.yml (verify tier: bench, NOT core)
  </read_first>
  <action>
Run the full validation suite across all personas per D-14. This is a verification-only task — no files modified. Run in sequence:

1. `./scripts/validate-personas.sh` (all personas — must exit 0 with no hard failures)
2. `grep -c '^tier: core$' persona-metadata/*.yml` (must return exactly 4)
3. `grep -c '^tier: bench$' persona-metadata/*.yml` (must return exactly 9: security-reviewer, finops-auditor, air-gap-reviewer, dual-deploy-reviewer + 5 new + junior-engineer)
4. `grep -c '^tier: chair$' persona-metadata/*.yml` (must return exactly 1: council-chair)
5. `grep -c '^tier: classifier$' persona-metadata/*.yml` (must return exactly 1: artifact-classifier)
6. Count bench persona slugs in commands/review.md bench enumeration section — must find all 9 signal-triggered bench slugs + junior-engineer always_invoke_on documentation

If any assertion fails, do NOT proceed — report the failure and stop. This is the gate before Wave 3 validation fixtures run.
  </action>
  <verify>
    <automated>./scripts/validate-personas.sh 2>&1; echo "validator exit: $?"; echo "core count:"; grep -c '^tier: core$' persona-metadata/*.yml; echo "bench count:"; grep -c '^tier: bench$' persona-metadata/*.yml; echo "chair count:"; grep -c '^tier: chair$' persona-metadata/*.yml</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/validate-personas.sh` exits 0 with zero hard failures across all 15 persona files (11 existing + 4 new agents that have sidecars... actually all 6 new have sidecars)
    - `grep -c '^tier: core$' persona-metadata/*.yml` outputs exactly `4`
    - `grep -c '^tier: bench$' persona-metadata/*.yml` outputs exactly `9`
    - `grep -c '^tier: chair$' persona-metadata/*.yml` outputs exactly `1`
    - `grep -c '^tier: classifier$' persona-metadata/*.yml` outputs exactly `1`
  </acceptance_criteria>
  <done>All personas pass validation. Core cardinality = 4. Bench cardinality = 9. Chair = 1. Classifier = 1. Total = 15 persona files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| commands/review.md conductor prompt | Shell-inject validator runs on every Write/Edit via PreToolUse hook (TD-04) |
| bin/dc-budget-plan.sh sidecar reading | Reads only from persona-metadata/*.yml inside REPO_ROOT; yq-based parsing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | commands/review.md conductor prompt | mitigate | Shell-inject validator (scripts/validate-shell-inject.sh) runs via PreToolUse hook on every Write/Edit to commands/*.md; TD-04 smoke-tested green |
| T-04-11 | Elevation of Privilege | always_invoke_on field injection | mitigate | Only reads from persona-metadata/*.yml inside REPO_ROOT using yq; field is a simple string array match against MANIFEST.artifact_type; no shell interpolation of field values |
| T-04-12 | Denial of Service | Budget cap bypass via always_invoke_on | accept | By design (D-06): always_invoke_on personas bypass budget cap. The number of such personas is controlled by sidecar authoring (currently only JE). Not a vulnerability — it's the intended behavior. |
</threat_model>

<verification>
- `./scripts/validate-personas.sh` exits 0 across all personas
- `grep -c '^tier: core$' persona-metadata/*.yml` = 4
- `grep -c '^tier: bench$' persona-metadata/*.yml` = 9
- `bash -n bin/dc-budget-plan.sh` exits 0
- All 9 bench slugs + junior-engineer appear in commands/review.md
</verification>

<success_criteria>
- Bench whitelist atomically expanded from 4 to 9 in commands/review.md
- Display-name map has all 14 entries (8 existing + 6 new)
- dc-budget-plan.sh reads always_invoke_on and auto-appends JE on code-diff
- JE bypasses budget cap (not counted against MAX_SPAWNABLE)
- Core cardinality stays at exactly 4
- Full validation passes across all 15 persona files
</success_criteria>

<output>
After completion, create `.planning/phases/04-six-personas-atomic-conductor-wiring/04-07-SUMMARY.md`
</output>
