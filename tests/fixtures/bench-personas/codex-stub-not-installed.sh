#!/usr/bin/env bash
# This stub is NEVER placed on PATH. The "not installed" case is simulated
# by running scripts/test-codex-delegation.sh with a PATH that contains no
# `codex` binary at all. Keeping this file as documentation of the case.
echo "This stub is intentionally never executed." >&2
exit 1
