image:
  repository: example/app
  tag: v1.0.0
  pullPolicy: IfNotPresent

replicas: 3

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  hosts:
    - host: app.example.com
      paths:
        - path: /
          pathType: Prefix
